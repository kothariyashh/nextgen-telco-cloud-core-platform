apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ngcmcp-nf.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "ngcmcp-nf.name" . }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "ngcmcp-nf.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
